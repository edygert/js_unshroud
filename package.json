{
  "name": "js_unshroud",
  "module": "index.ts",
  "type": "module",
  "version": "0.0.5",
  "description": "Headless JavaScript monitoring and analysis tool",
  "author": "Evan H. Dygert",
  "license": "MIT",
  "bin": {
    "js_unshroud": "./dist/runner.js"
  },
  "scripts": {
    "build": "bun build ./src/cli/runner.ts --compile --outfile dist/js_unshroud",
    "build:linux": "bun build ./src/cli/runner.ts --compile --outfile dist/js_unshroud-linux-x64 --target bun-linux-x64",
    "build:macos": "bun build ./src/cli/runner.ts --compile --outfile dist/js_unshroud-macos-x64 --target bun-macos-x64",
    "build:macos-arm": "bun build ./src/cli/runner.ts --compile --outfile dist/js_unshroud-macos-arm64 --target bun-macos-arm64",
    "build:windows": "bun build ./src/cli/runner.ts --compile --outfile dist/js_unshroud-windows-x64.exe --target bun-windows-x64",
    "build:all": "npm run build:linux && npm run build:macos && npm run build:macos-arm && npm run build:windows",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "lint": "eslint src/ tests/ --ext .ts,.js",
    "lint:fix": "eslint src/ tests/ --ext .ts,.js --fix",
    "dev": "bun run src/cli/runner.ts"
  },
  "dependencies": {
    "playwright-core": "^1.57.0",
    "devtools-protocol": "^0.0.1566079"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@typescript-eslint/eslint-plugin": "^8.19.2",
    "@typescript-eslint/parser": "^8.19.2",
    "@vitest/coverage-v8": "^4.0.16",
    "chromium-bidi": "^0.4.7",
    "electron": "^26.0.0",
    "eslint": "^9.17.0",
    "typescript": "^5",
    "vitest": "^4.0.16"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
